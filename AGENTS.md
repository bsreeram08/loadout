## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `bsreeram08/loadout` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default five-label triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain layout: root `CONTEXT.md` plus root `docs/adr/`. See `docs/agents/domain.md`.
