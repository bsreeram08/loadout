# Loadout — load active env profile (idempotent; safe to append once)
if command -v loadout >/dev/null 2>&1; then
  eval "$(loadout export 2>/dev/null)"
  reloadenv() { eval "$(loadout export 2>/dev/null)"; }
fi